# Application shortcuts
alias j='jobs'
alias m='mpv'
alias r='ranger'
alias ranger='ranger --choosedir=$HOME/.rangerdir; LASTDIR=$(cat $HOME/.rangerdir); cd "$LASTDIR"'
alias trc='transmission-remote-cli'
alias v='nvim'
alias xup='xrdb .Xresources'
alias z=zathura

# Vim
alias vim='nvim'
alias vi='nvim'
alias v='nvim'

# Package managers
# alias pip='sudo pip'
alias apt='sudo apt-get'
alias p='paru'

# Screen
alias s='screen'
alias ss='screen -S'
alias sr='screen -r'

# Source config files
alias sc='source $HOME/.zshrc'
alias sp='source $HOME/.zprofile'

# Weather forecast
alias wttr='curl wttr.in/Grenoble+38000'

# zotcli
alias zr='zotcli read'
alias zq='zotcli query'
alias zen='zotcli edit-note'
